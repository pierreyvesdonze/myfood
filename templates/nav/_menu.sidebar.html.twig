<nav class="menu">
    <button class="openbtn">
        <i class="fa fa-bars fa-2x"></i>
    </button>

    <div class="filters-sliders">
        <i class="fa fa-sliders-h fa-2x"></i>
    </div>

<!--     <div class="search">

        <form action="" class="search" placeholder="&#xF002;" method="POST">
            <i class="fa fa-search"></i>
            <input type="text" class="search-input">
        </form>

    </div> -->

    <div class="search">
     
       {{ render(controller('App\\Controller\\SearchController::searchBarAction')) }}
   
    </div>


    <div id="mySidepanel" class="sidepanel">
        <a href="#" class="closebtn">&times;</a>

        {# LEFT #}
        <div class="sidepanel-container sidepanel-left">

            <div class="homepage-link">
                <a href="{{ path('homepage') }}">Accueil</a>
            </div>

            {% if app.user %}
            <div class="recipies-link">
                <a href="{{ path('recipe_list') }}">Mes recettes</a>
                {#        <a href="{{ path('recipe_add') }}">
                        <i class="fa fa-plus-circle" aria-hidden="true"></i>
                    </a> #}
            </div>

            <div class="shopping-list-link">
                <a href="{{ path('shopping_list_list') }}">Liste de courses</a>
            </div>
            {% endif %}
        </div>

        {# RIGHT #}
        <div class="sidepanel-container sidepanel-right">

            {% if app.user %}

            <div class="meal-plan-link">
                <a href="{{ path('meal_plan_add') }}">Planning</a>
            </div>

            <div class="fridge-link">
                <a href="{{ path('search_list_by_ingredients') }}">Mon frigo</a>
            </div>


            <div class="log-link">
                <a href="{{ path('logout') }}">Déconnexion</a>
            </div>

            {% else %}
            <div class="log-link">
                <a href="{{ path('login') }}">Se connecter</a>
            </div>

            <div class="register-link">
                <a href="{{ path('new') }}">S'inscrire</a>
            </div>
            {% endif %}
        </div>
    </div>
</nav>