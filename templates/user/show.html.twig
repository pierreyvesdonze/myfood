{% extends 'base.html.twig' %}

{% block title %}Profil perso
{% endblock %}

{% block body %}
	<div class="myprofile">
		<table class="table">
			<tbody>
				<tr>
					<th>Pseudo</th>
					<td>{{ user.username }}</td>
				</tr>
				<tr>
					<th>Email</th>
					<td>{{ user.email }}</td>
				</tr>
			</tbody>
		</table>

		<hr class="hr">

		<div class="control">
			<a href="{{ path('user_edit_profile') }}" class="btn btn-primary">
				Réinitialiser le mdp
			</a>

			{{ include('user/_delete_form.html.twig') }}
		</div>

	</div>
{% endblock %}
